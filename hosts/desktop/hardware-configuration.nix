# This placeholder lets the flake evaluate before the first real install.
# nixos-anywhere can overwrite it with detected hardware data:
#   --generate-hardware-config nixos-generate-config ./hosts/desktop/hardware-configuration.nix
{ ... }:

{
}
